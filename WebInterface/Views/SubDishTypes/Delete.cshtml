@model WebInterface.ViewModel.SubDishTypesErrorViewModel

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.subDishType)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.subDishType)
        </dd>
        <dd>
            @if (Model.error != null)
            {
                Html.DisplayFor(model => model.error);
            }
        </dd>

    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="subDishType.SubDishTypeID" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
